package enums;

//  If we have a website with limited selection menu, but we have OTHER enum if we get
    //  an open position which doesn't correspond to current available industry or
    //  applicant which would like to
    // send us his application "for future" (which we'll keep as PENDING)
    public enum Industry {
        TECH, FOOD, RETAIL, CRYPTO, REAL_ESTATE, OTHER
    }

